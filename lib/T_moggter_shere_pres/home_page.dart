import 'package:flutter/material.dart';

class HomePagePre extends StatelessWidget {
  const HomePagePre({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("home page"),
      ),
    );
  }
}
